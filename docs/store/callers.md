## 呼叫器

```javascript
{
  id:{
    id: '', // 呼叫器唯一编码 string
    type:'caller', // 资源类型
    title: '', // 名称 string
    memo: '', // 备注 string
    name: 'rf433', // 呼叫器的名称,存在多种呼叫器,默认rf433 string
    card: '', // 呼叫器绑定的卡片 number
    keyMap: null, // 按键定义,默认为空,可自定义
  }
}
```
