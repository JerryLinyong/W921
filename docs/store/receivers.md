## 接收器

```javascript
{
  id:{
    id: '', // 接收器唯一编码 string
    type:'receiver', // 资源类型
    name: 'screen', // 接收器的名称,存在多种接收器,默认screen string
    title: '', // 名称 string
    memo: '', // 备注 string
    card: '', // 接收器绑定的卡片 number
  }
}
```
